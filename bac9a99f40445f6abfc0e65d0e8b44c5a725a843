Spec: specs/000-foundation/spec.md (AC13, AC14, AC17)
Why this approach: test routes go through the same middleware chain as /healthz instead of a debug route or a build-tagged binary (plan.md, Test-only routes).
Verified by: GOFLAGS=-count=1 make verify; go test -race -count=5 ./cmd/gateway. TestRun_AllLinesJSON (startup, 200, 404, recovered panic, shutdown; every line JSON with level, ts, msg; request lines have a 26-char request_id), TestRun_SecretsNotLogged (sentinel in Authorization, x-api-key and ?key= on /healthz and a 404, at debug), TestRun_PanicRecovered (500, then /healthz 200, one panic recovered line, stderr pipe empty; os.Stderr swapped before run builds the logger, per Q6). Mutations caught: the recoverer writing plain text to stderr fails PanicRecovered; the access log including the query fails SecretsNotLogged.
