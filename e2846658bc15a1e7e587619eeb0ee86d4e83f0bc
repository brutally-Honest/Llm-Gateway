Why this approach: the T3 review could not run its revert check because the hook allowed only git reads and make verify; go test is the narrowest command that makes that check possible.
Trade-off / known limit: go test still runs the repo's own test code, which the reviewer does not otherwise execute.
Verified by: agent-bash-guard.sh run against sample allowed, file-writing (-o, -o=, -outputdir, profiles, -trace, --output), program-running (-exec, -toolexec), chained and unknown commands for the reviewer, plus implementer commands to confirm they are unaffected.
