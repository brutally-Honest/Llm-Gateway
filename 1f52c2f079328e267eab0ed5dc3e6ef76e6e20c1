Spec: specs/000-foundation/spec.md (fail fast before binding); plan.md step 2
Why this approach: -config is the only way to name a file; logging only the count keeps a path or anything else a user typed out of the logs.
Verified by: GOFLAGS=-count=1 make verify; TestRun_InvalidFlags/{unknown_flag,positional,positional_after_flag}. With run.go at b26ccc6, positional and positional_after_flag fail and unknown_flag passes.
